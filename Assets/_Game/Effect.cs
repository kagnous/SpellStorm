using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Effect : MonoBehaviour
{
    /*private int coup = 10; public int Coup { get { return coup; } set { coup = value; } }
    private int cadence = 1; public int Cadence { get { return cadence; } set { cadence = value; } }

    private StatsManager target; public StatsManager Target { get { return target; } set { target = value; } }

    private TokenEffect effet; public TokenEffect Effet { get { return effet; } set { effet = value; } }

    void Start()
    {
        StartCoroutine(BurnCoroutine());
    }

    IEnumerator BurnCoroutine()
    {
        for (int i = 0; i < coup; i++)
        {
            yield return new WaitForSeconds(cadence);
            target.HP -= 1;
            Debug.Log("-1 HP");
        }
        target.Effects.Remove(effet);
        Destroy(gameObject);
    }*/
}